import { AuthCurrentDirective } from './auth-current.directive';

describe('AuthCurrentDirective', () => {
  it('should create an instance', () => {
    const directive = new AuthCurrentDirective();
    expect(directive).toBeTruthy();
  });
});
